# ================================
# Application Configuration
# ================================
spring.application.name=ASpringBootFirstDemoProject
server.port=8085

# ================================
# Database Configuration (MySQL)
# ================================
spring.datasource.url=jdbc:mysql://localhost:3306/customer_db?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ================================
# JPA / Hibernate Settings
# ================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ================================
# Custom Messages
# ================================

# Customer Messages
API.INSERT_SUCCESS=CUSTOMER ADDED SUCCESSFULLY (Created by Ajay)
API.UPDATE_SUCCESS=CUSTOMER RECORD UPDATED SUCCESSFULLY
API.DELETE_SUCCESS=CUSTOMER RECORD DELETED SUCCESSFULLY
Service.CUSTOMERS_NOT_FOUND=CUSTOMER RECORD NOT FOUND

# Employee Messages
EMP.INSERT_SUCCESS=EMPLOYEE ADDED SUCCESSFULLY
EMP.UPDATE_SUCCESS=EMPLOYEE RECORD UPDATED SUCCESSFULLY
Service.EMPLOYEE_NOT_FOUND=EMPLOYEE RECORD NOT FOUND

# General Messages
General.EXCEPTION_MESSAGE=This is the General Exception
